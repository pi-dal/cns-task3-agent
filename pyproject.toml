[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "cns-task3-agent"
version = "0.1.0"
description = "AI agent for CNS Task 3: Protein Conformational Ensemble Generation"
requires-python = ">=3.10"
dependencies = []
